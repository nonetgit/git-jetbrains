# 5️⃣ Первый репозиторий

<div align="right">
  <a href="04-jetbrains-interface.md">⬅️ Предыдущая</a> | 
  <a href="06-file-lifecycle.md">➡️ Следующая</a>
</div>

---

## 📁 Что такое репозиторий

**Репозиторий** — это обычная папка на вашем компьютере, за которой следит Git.

Внутри этой папки Git создает скрытую подпапку `.git`, где хранит:
- Всю историю изменений
- Настройки репозитория
- Информацию о ветках
- Указатели на коммиты

> **Важно:** Папку `.git` никогда не нужно трогать вручную. Это служебная директория Git. Если её удалить, история изменений пропадет.

---

## 🆕 Создание нового репозитория в JetBrains

### Способ 1: Создать новый проект и сразу сделать его репозиторием

1. **File → New → Project**
2. Выберите тип проекта (Python, C++, C# и т.д.)
3. Укажите имя и расположение
4. **Важно:** Включите опцию **"Create Git repository"**

> *Изображение: Create New Project with Git (assets/images/new-project-with-git.png)*

**Где находится эта опция:**
- В IntelliJ IDEA: на последнем шаге создания проекта
- В PyCharm: в диалоге создания проекта (галочка внизу)
- В CLion: в настройках проекта
- В Rider: в мастере создания решения

Готово! Проект создан и уже под Git.

---

### Способ 2: Сделать Git-репозиторием существующий проект

1. Откройте существующий проект
2. В верхнем меню: **VCS → Enable Version Control Integration...**
3. В выпадающем списке выберите **Git**
4. Нажмите **OK**

> *Изображение: Enable VCS (assets/images/enable-vcs.png)*

Теперь проект под контролем Git.

---

### Способ 3: Через терминал

Можно и через терминал, но в JetBrains это редко нужно:

```bash
# Перейдите в папку проекта
cd /путь/к/проекту

# Инициализируйте репозиторий
git init
```

После этого в JetBrains нужно будет **File → Open** и выбрать эту папку.

---

## 👀 Что изменилось после создания репозитория

### В файловой системе:
Появилась скрытая папка `.git` в корне проекта

### В JetBrains:

1. **Цвет файлов** в Project View:
   - Файлы стали **красными** (они еще не в Git)
   - Папка `.git` не видна (скрытая)

2. **Появилась кнопка Git** слева внизу:
   - `Alt + 9` теперь открывает окно Git

3. **В меню VCS** появились пункты:
   - Commit...
   - Push/Pull...
   - Branches...

4. **В правом верхнем углу** появились иконки:
   - ✅ Commit (`Ctrl + K`)
   - 🔄 Update (`Ctrl + T`)
   - ⬆️ Push (`Ctrl + Shift + K`)
   - 🌿 Branches (``Ctrl + Shift + ` ``)

---

## 📄 .gitignore — что не нужно хранить в Git

Не все файлы должны попадать в репозиторий. Например:

- **Скомпилированные файлы:** `*.class`, `*.pyc`, `*.exe`, `*.obj`
- **Папки сборки:** `build/`, `dist/`, `target/`, `out/`
- **Зависимости:** `node_modules/`, `venv/`, `packages/`
- **Настройки IDE:** `.idea/`, `.vscode/` (кроме общих)
- **Логи и временные файлы:** `*.log`, `*.tmp`, `*.cache`
- **Секреты:** `.env`, `secrets.json`, `config/database.yml`

---

### Как создать `.gitignore` в JetBrains

**Способ 1: Через New → File**
1. Правый клик на корне проекта
2. **New → File**
3. Назовите файл **`.gitignore`**
4. Добавьте правила (см. примеры ниже)

**Способ 2: Через плагин .ignore (рекомендуется)**
1. `File → Settings → Plugins`
2. Найдите и установите **.ignore**
3. Правый клик на корне проекта → **New → .ignore file → .gitignore**
4. Выберите шаблон под ваш язык (Python, C++, C#)
5. Плагин сам добавит стандартные правила

> **Совет:** Файл `.gitignore` нужно создавать сразу, до первого коммита. Если добавить в Git ненужные файлы, потом их будет сложно удалить из истории.

---

## 🐍 Пример .gitignore для Python

```gitignore
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
venv/
env/
ENV/
.venv/
virtualenv/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
logs/

# Jupyter Notebook
.ipynb_checkpoints/

# pytest
.pytest_cache/
htmlcov/
.coverage

# Distribution / packaging
dist/
build/
*.egg-info/

# Database
*.db
*.sqlite3

# Environment variables
.env
.env.local
```

---

## ➕ Пример .gitignore для C++

```gitignore
# Compiled files
*.o
*.obj
*.exe
*.out
*.app
*.dll
*.so
*.dylib

# Build directories
build/
cmake-build-*/
out/
bin/
obj/

# IDE
.idea/
.vscode/
*.swp
*.swo
.cache/
.clangd/

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
CTestTestfile.cmake

# Visual Studio
.vs/
*.sln
*.vcxproj
*.vcxproj.filters
*.user
*.suo

# Logs
*.log

# Profiling
*.profraw
*.gcda
*.gcno
```

---

## #️⃣ Пример .gitignore для C# (.NET)

```gitignore
# Build results
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
build/
bld/
[Bb]in/
[Oo]bj/

# Visual Studio
.vs/
*.user
*.userosscache
*.suo
*.userprefs

# NuGet
packages/
*.nupkg
project.lock.json

# ASP.NET
App_Data/
*.log
*.tmp

# Entity Framework
*.edmx.sql

# VS Code
.vscode/

# Rider
.idea/

# Logs
*.log
logs/

# Publish
publish/
pub/

# Test results
TestResults/
*.trx

# User-specific files
*.rsuser
*.suo
*.user
*.userosscache

# JetBrains Rider
.idea/
*.sln.iml
```

---

## 📋 Проверка статуса

Чтобы увидеть, что Git видит в вашем проекте:

### В JetBrains:
- Посмотрите на **цвета файлов** в Project View
- Или откройте `Alt + 9` → вкладка **Local Changes**

Там будет список всех изменений с разделением на:
- **Unversioned Files** — новые файлы, не в Git
- **Changed Files** — измененные файлы
- **Deleted Files** — удаленные файлы

### В терминале:
```bash
git status
```

Покажет:
- Какие файлы не отслеживаются (красные)
- Какие файлы изменены (синие)
- Какие готовы к коммиту (зеленые)

---

## 🎯 Резюме

Теперь у вас есть:

| Что сделали | Как проверить |
|-------------|---------------|
| ✅ Репозиторий Git | Есть папка `.git` в корне |
| ✅ Понимание интерфейса | Кнопка Git слева внизу |
| ✅ `.gitignore` для Python/C++/C# | Файл в корне с правилами |
| ✅ Понимание статусов | Красные файлы — новые |

**Следующий шаг:** Научиться делать коммиты и понять жизненный цикл файлов в Git.

---

<div align="center">

[📚 В словарь](GLOSSARY.md) | [🏠 На главную](README.md)

</div>
