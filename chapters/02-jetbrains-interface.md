# 4️⃣ Интерфейс Git в JetBrains

<div align="right">
  <a href="03-installation.md">⬅️ Предыдущая</a> | 
  <a href="05-first-repo.md">➡️ Следующая</a>
</div>

---

## 🎯 Где искать Git в JetBrains

В любой IDE от JetBrains (IntelliJ IDEA, PyCharm, WebStorm, PhpStorm, RubyMine) интерфейс Git выглядит одинаково. Это огромный плюс — изучив один раз, вы сможете работать в любой среде.

---

## 📌 Главные элементы интерфейса

### 1. Git Tool Window (Окно Git)

**Как открыть:**
- `Alt + 9` (Windows/Linux)
- `Cmd + 9` (macOS)
- Или кнопка **Git** в левом нижнем углу

> *Примечание: Если вы не видите кнопку Git в левом нижнем углу, нажмите на квадратик с точками (View → Tool Windows) и найдите Git там.*

**Вкладки внутри:**
- **Commit** — создание коммитов и просмотр изменений
- **Log** — история изменений с графом веток
- **Local Changes** — только незакоммиченные изменения
- **Console** — вывод Git команд (что именно делает IDE)

---

### 2. VCS Menu (Меню Version Control)

Верхнее меню: **VCS** (в новых версиях) или **Git** (в старых версиях)

Содержит все основные операции:
- Commit...
- Push...
- Pull...
- Branches...
- History...
- Stash...
- Rollback...

---

### 3. Горячие клавиши (самые важные)

| Действие | Windows/Linux | macOS |
|----------|--------------|-------|
| **Commit** | `Ctrl + K` | `Cmd + K` |
| **Push** | `Ctrl + Shift + K` | `Cmd + Shift + K` |
| **Update/Pull** | `Ctrl + T` | `Cmd + T` |
| **Branches** | `Ctrl + Shift + \`` | `Cmd + Shift + \`` |
| **History** | `Alt + 9` → Log | `Cmd + 9` → Log |
| **Annotate (Blame)** | Правый клик на полях → Git → Annotate | То же |
| **Revert** | `Ctrl + Alt + Z` | `Cmd + Alt + Z` |
| **Compare with branch** | Правый клик → Git → Compare with branch | То же |
| **Show Diff** | `Ctrl + D` (в окне коммита) | `Cmd + D` |

> **Обратите внимание:** Для Branches используется сочетание ``Ctrl + Shift + ` `` (Windows/Linux) и ``Cmd + Shift + ` `` (macOS). Обратная кавычка находится на одной клавише с тильдой (~), обычно слева от цифры 1.

---

### 4. Цвета файлов в Project View

В окне проекта (где лежат ваши папки и файлы) файлы подсвечиваются разными цветами. Это очень удобно для быстрой оценки состояния:

| Цвет | Значение | Пример |
|------|----------|---------|
| 🟢 **Зеленый** | Новый файл, еще не добавленный в Git (Untracked) | Только что создали |
| 🔵 **Синий** | Файл изменен, но не закоммичен (Modified) | Поправили код |
| ⚫ **Черный** | Файл без изменений (Unmodified) | Всё ок, трогать не надо |
| 🔴 **Красный** | Файл будет удален при коммите (Deleted) | Пометили на удаление |
| 🟡 **Желтый** | Файл игнорируется Git (.gitignore) | Логи, кэш, сборки |

> **Совет:** Если цвета не отображаются, проверьте настройки: File → Settings → Version Control → File Status Colors

---

### 5. Кнопки на панели инструментов

На верхней панели инструментов справа есть иконки для быстрого доступа:

| Иконка | Действие | Горячая клавиша |
|--------|----------|-----------------|
| 🔄 | **Update** (git pull) | `Ctrl + T` |
| ✅ | **Commit** | `Ctrl + K` |
| ⬆️ | **Push** | `Ctrl + Shift + K` |
| 🌿 | **Branches** | `Ctrl + Shift + \`` |
| 📋 | **History** (текущего файла) | Правый клик → Git → Show History |

> *Если вы не видите эти кнопки, включите их: View → Appearance → Toolbar*

---

## 📝 Окно Commit

**Как открыть:** `Ctrl + K` или кнопка ✅ на панели инструментов

> *Изображение: Commit Window (assets/images/commit-window.png)*

### Состав окна:

1. **Список изменений** (левая панель)
   - Галочками отмечаем, какие файлы попадут в коммит
   - Цвета соответствуют статусу файлов (см. таблицу выше)
   - Можно открыть файл двойным кликом

2. **Diff вьювер** (правая панель)
   - Показывает изменения в выделенном файле
   - Зеленое — добавленный код
   - Красное — удаленный код
   - Синее — измененный код

3. **Commit Message** (нижняя панель)
   - Поле для сообщения коммита
   - Проверка правописания (красная волнистая линия)
   - Можно вставить предыдущие сообщения (стрелочка вниз)
   - Подсказка: пишите в повелительном наклонении ("Добавить", "Исправить", а не "Добавил")

4. **Кнопки действий**
   - **Commit** — сделать коммит (только локально)
   - **Commit and Push** — коммит + отправка на сервер
   - **Amend** — исправить последний коммит (добавить изменения в предыдущий коммит)

---

## 📊 Окно Log (История)

**Как открыть:** `Alt + 9` → вкладка Log

> *Изображение: Log Window (assets/images/log-window.png)*

### Что можно увидеть:

- **Граф коммитов** — визуализация веток (как дерево)
- **Список коммитов** — все сохранения с датами и авторами
- **Детали коммита** — изменения, автор, дата, хеш
- **Фильтры** — по автору, дате, тексту сообщения

### Полезные действия (правый клик на коммите):

| Действие | Что делает |
|----------|------------|
| **Checkout** | Переключиться на этот коммит (detached HEAD) |
| **Create Branch** | Создать ветку от этого коммита |
| **Cherry-pick** | Взять этот коммит в текущую ветку |
| **Reset Current Branch to Here** | Откатить ветку до этого коммита |
| **Show Repository at Revision** | Показать, как выглядел проект в этом коммите |

> **Важно:** Checkout на старый коммит переведет вас в состояние "detached HEAD". Чтобы вернуться, просто переключитесь обратно на ветку (например, `main`).

---

## 🔍 Annotate (Кто написал эту строку)

**Как включить:**
1. Откройте любой файл с кодом
2. Правый клик на полях слева (рядом с номерами строк)
3. Выберите **Git → Annotate**

> *Изображение: Annotate (assets/images/annotate.png)*

**Что появится:**
Слева от каждой строки будет панель с информацией:
- Имя автора (кто последним менял строку)
- Дата коммита
- Хеш коммита (короткий, 7-8 символов)

**Дополнительные возможности:**
- Клик по аннотации → показать детали коммита
- Наведение мыши → всплывающая подсказка с сообщением коммита
- Еще раз правый клик → **Annotate Previous** (кто менял до этого)

> Это незаменимая вещь, когда нужно спросить у коллеги: "Ты зачем этот код написал?" 😊

---

## ⚡ Git Actions Menu (Быстрый поиск команд)

В любом месте IDE можно нажать `Shift` два раза (`Shift + Shift`) — откроется окно поиска "Search Everywhere".

Переключитесь на вкладку **Actions** и начните вводить Git-команду:

> *Изображение: Git Actions (assets/images/git-actions.png)*

**Примеры:**
- `commit` — откроет окно коммита
- `push` — выполнит push
- `branch` — покажет список веток
- `stash` — спрятать изменения
- `pull` — обновить проект

> Это быстрее, чем искать нужный пункт в меню!

---

## 🔧 Настройки Git в JetBrains

**File → Settings → Version Control → Git**

Или просто нажмите `Ctrl + Alt + S` и в поиске введите "Git".

> *Изображение: Git Settings (assets/images/git-settings.png)*

### Важные настройки:

| Настройка | Рекомендация | Зачем |
|-----------|--------------|-------|
| **Path to Git executable** | Оставить как есть (автоопределение) | Путь к `git.exe` |
| **SSH executable** | Built-in | Встроенный SSH-клиент |
| **Auto-update if push rejected** | ✅ Включить | При конфликте сделает pull |
| **Use credential helper** | ✅ Включить | Сохранять пароли |
| **Fetch periodically** | ✅ Включить | Автоматически проверять изменения на сервере |

---

## 📋 Console (Вывод Git команд)

Всегда можно посмотреть, какие именно Git-команды выполняет IDE:

**Как открыть:** `Alt + 9` → вкладка **Console**

**Что там видно:**
- Какие команды Git выполняются (`git commit`, `git push` и т.д.)
- Результат выполнения (success/fail)
- Ошибки, если что-то пошло не так

> Полезно, когда что-то работает неожиданно — можно увидеть реальные команды, которые IDE отправляет в Git.

---

## 🎯 Резюме (что нужно запомнить)

Для эффективной работы с Git в JetBrains достаточно знать:

| Действие | Где искать |
|----------|------------|
| **Сделать коммит** | `Ctrl + K` |
| **Отправить на сервер** | `Ctrl + Shift + K` |
| **Обновить проект** | `Ctrl + T` |
| **Посмотреть историю** | `Alt + 9` → Log |
| **Узнать автора строки** | Правый клик на полях → Annotate |
| **Переключить ветку** | ``Ctrl + Shift + ` `` → выбрать ветку |
| **Посмотреть цвета файлов** | В окне проекта |

**Главное преимущество:** В IDE есть ВСЁ для работы с Git. Терминал нужен только для очень сложных или редких операций. 90% времени вы будете работать через интерфейс JetBrains.

---

<div align="center">

[📚 В словарь](https://github.com/nonetgit/git-jetbrains-course/blob/main/GLOSSARY.md) | [🏠 На главную](README.md)

</div>
