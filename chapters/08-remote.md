# 1️⃣0️⃣ Удаленные репозитории (Remote)

<div align="right">
  <a href="09-merging.md">⬅️ Предыдущая</a> | 
  <a href="11-scenarios.md">➡️ Следующая</a>
</div>

---

## ☁️ Что такое удаленный репозиторий

**Удаленный репозиторий (remote)** — это копия проекта, которая хранится не на вашем компьютере, а на сервере.

Популярные платформы:
- **GitHub** — самый популярный (open-source)
- **GitLab** — с CI/CD и DevOps
- **Bitbucket** — интеграция с Jira
- **Azure DevOps** — от Microsoft

---

## 🗺️ Схема работы

```mermaid
graph LR
    A[Локальный репозиторий<br/>Ваш компьютер] -->|git push| B[Удаленный репозиторий<br/>GitHub/GitLab]
    B -->|git pull| A
    C[Другой разработчик] -->|git push| B
    B -->|git pull| C
```

---

## 📋 Основные операции

### Pull — забрать изменения с сервера
### Push — отправить изменения на сервер
### Clone — скопировать удаленный репозиторий к себе
### Fetch — проверить изменения без слияния

---

## 🔌 Настройка связи с удаленным репозиторием

### 1. Создать репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите **New** (зеленая кнопка)
3. Введите имя репозитория (например, `my-project`)
4. Можно добавить README (но лучше без, если уже есть локальный)
5. **Create repository**

---

### 2. Привязать локальный репозиторий к удаленному

В JetBrains:

**Способ 1: Через меню**
1. **VCS → Git → Remotes...**
2. Нажмите **+** (Add)
3. Введите имя: `origin` (стандартное)
4. Вставьте URL репозитория (с GitHub)
5. **OK**

**Способ 2: Через терминал (иногда проще)**
```bash
git remote add origin https://github.com/username/my-project.git
```

---

### Где взять URL:

На странице репозитория GitHub есть кнопка **Code**:
- HTTPS: `https://github.com/username/repo.git`
- SSH: `git@github.com:username/repo.git` (требует ключи)

---

## ⬆️ Push (отправка на сервер)

### Первый push (связывание веток):

1. **VCS → Git → Push** (или `Ctrl + Shift + K`)
2. Выберите ветку для отправки
3. Нажмите **Push**

**Важно:** При первом push нужно связать локальную ветку с удаленной:
- В диалоге push есть опция "Set upstream"
- Или флажок "Push current branch to origin and set upstream"

После этого можно будет просто нажимать Push.

---

### Что такое upstream?

**Upstream** — это связь между локальной веткой и удаленной:
- Локальная `main` → удаленная `origin/main`
- Git запоминает, куда отправлять и откуда забирать

---

## ⬇️ Pull (забрать с сервера)

### Получить изменения из удаленного репозитория:

1. **VCS → Git → Pull** (или `Ctrl + T`)
2. Выберите ветку, откуда тянуть (обычно `origin/main`)
3. Нажмите **Pull**

### Что делает Pull:
1. `git fetch` — получает данные с сервера
2. `git merge` — сливает с локальной веткой

Может возникнуть конфликт, как при обычном merge.

---

## 🆕 Clone (копирование репозитория)

Если проект уже есть на GitHub, а у вас его нет локально:

1. **File → New → Project from Version Control**
2. Вставьте URL репозитория
3. Выберите папку для сохранения
4. **Clone**

Или через **VCS → Get from Version Control**

---

## 🔍 Fetch (проверить изменения)

**Fetch** — получить информацию о новых коммитах, но не сливать их:

1. **VCS → Git → Fetch**
2. В Log появятся новые коммиты (удаленные ветки подсвечены)
3. Решаете сами, сливать или нет

---

## 🌐 Работа с несколькими удаленными репозиториями

Можно добавить несколько remote:

```
origin    → ваш форк на GitHub
upstream → оригинальный репозиторий
```

### Добавить второй remote:
**VCS → Git → Remotes → +**

```bash
git remote add upstream https://github.com/original/project.git
```

### Забрать из upstream:
```bash
git pull upstream main
```

---

## 🔐 Аутентификация

### HTTPS (пароль)
- Раньше: логин + пароль
- Сейчас: **Personal Access Token** (GitHub)

### Как создать токен на GitHub:
1. Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token
3. Выбрать права (repo — все)
4. Скопировать токен
5. При push ввести токен вместо пароля

### SSH (ключи)
Безопаснее, не требует ввода пароля:

1. Создать ключ: `ssh-keygen -t rsa -b 4096`
2. Добавить в GitHub: Settings → SSH and GPG keys
3. Использовать SSH URL при clone

В JetBrains можно настроить в Settings → Version Control → Git → SSH executable: Native

---

## 📊 Визуализация в Log

В окне Log:
- **main** — ваша локальная ветка
- **origin/main** — удаленная ветка (сервер)
- Цвета помогают различать

Если вы отстали:
```
main: ○──○
origin/main: ○──○──○──○
```

Надо сделать pull.

---

## 🧵 Типичные ошибки

### "Push rejected"
Причина: на сервере есть коммиты, которых нет у вас.

Решение:
1. Pull (или Pull with Rebase)
2. Разрешить конфликты
3. Push снова

### "Failed to push: failed to push some refs to"
Та же причина — сервер впереди.

### "Could not read from remote repository"
Проблемы с доступом:
- Неправильный URL
- Нет прав
- Проблемы с SSH

---

## 💡 Хорошие практики

1. **Pull перед началом работы**
   - Всегда забирайте последние изменения

2. **Частые push**
   - Не держите изменения только локально

3. **Понятные сообщения коммитов**
   - Помогают команде

4. **Не push в main напрямую**
   - Используйте Pull Requests (на GitHub)

5. **Проверяйте, что пушите**
   - Не отправляйте секреты (пароли, ключи)

---

## 🔄 Pull Requests (запросы на слияние)

В GitHub/GitLab это не Git-команда, а веб-интерфейс:

1. Вы пушите свою ветку (`feature/login`)
2. На GitHub создаете Pull Request
3. Команда обсуждает изменения
4. После одобрения — merge

---

## 🎯 Резюме

- ✅ Удаленный репозиторий — серверная копия
- ✅ Push — отправить изменения
- ✅ Pull — забрать изменения
- ✅ Clone — скопировать репозиторий
- ✅ Origin — стандартное имя для основного remote
- ✅ Pull Request — способ предложить изменения

---

<div align="center">
  
[📚 В словарь](GLOSSARY.md) | [🏠 На главную](README.md)

</div>
