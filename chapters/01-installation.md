# 3️⃣ Установка и настройка Git (Windows)

<div align="right">
  <a href="02-concepts.md">⬅️ Предыдущая</a> | 
  <a href="04-jetbrains-interface.md">➡️ Следующая</a>
</div>

---

## 🖥️ Установка Git на Windows

### Способ 1: Официальный установщик (рекомендуется)

1. Перейдите на официальный сайт: [git-scm.com/download/win](https://git-scm.com/download/win)
2. Скачайте установщик (сайт автоматически предложит версию под вашу систему)
3. Запустите скачанный `.exe` файл
4. **Важно:** Если появится запрос от Контроля учетных записей (UAC), нажмите "Да"

### Пошаговая инструкция по установке

При установке вам будут предложены настройки. Вот оптимальные параметры для Windows-пользователя:

| Экран | Что выбрать | Комментарий |
|-------|-------------|-------------|
| **Information** | Просто читаем лицензию | Нажимаем Next |
| **Select Destination Location** | Оставляем как есть | Обычно `C:\Program Files\Git` |
| **Select Components** | **Оставляем все по умолчанию** | Обязательно оставьте галочку на **Git Bash Here** и **Git GUI Here** — это добавит удобные пункты в контекстное меню |
| **Select Start Menu Folder** | Оставляем как есть | Нажимаем Next |
| **Choosing the default editor** | Выберите **Use Visual Studio Code as Git's default editor** | *Если у вас установлен VS Code. Если нет — оставьте Vim или выберите другой редактор из списка* |
| **Adjusting your PATH environment** | Выбираем **Git from the command line and also from 3rd-party software** | Это позволит использовать Git не только в Git Bash, но и в обычной командной строке (cmd) и PowerShell |
| **Choosing HTTPS transport backend** | Оставляем **Use the OpenSSL library** | Стандартный и надежный вариант |
| **Configuring the line ending conversions** | Выбираем **Checkout Windows-style, commit Unix-style line endings** | **Важно:** Это правильная настройка для Windows, чтобы избежать проблем с переносом строк |
| **Configuring the terminal emulator** | Выбираем **Use MinTTY** | Более удобный терминал с нормальной поддержкой цветов и истории |
| **Choose pull behavior** | Оставляем **Default (fast-forward or merge)** | Подходит для большинства пользователей |
| **Choose a credential helper** | Оставляем **Git Credential Manager** | Будет сохранять пароль от GitHub/GitLab, чтобы не вводить каждый раз |
| **Configuring extra options** | Оставляем все галочки | Включите **Enable file system caching** (ускоряет работу) |
| **Experimental options** | Ничего не выбираем | Экспериментальные функции могут быть нестабильны |

После последнего экрана начнется установка. По завершении снимите галочку "View Release Notes" и нажмите **Finish**.

### Способ 2: Через пакетный менеджер (для продвинутых)

**Через winget (встроен в Windows 10/11):**
```bash
# Поиск пакета (если не уверены в названии)
winget search git

# Установка
winget install --id Git.Git -e --source winget
```

**Через Chocolatey (если установлен):**
```bash
choco install git
```

---

## ✅ Проверка установки

После установки нужно убедиться, что всё работает.

1. **Откройте Git Bash:**
   - Нажмите `Пуск`, найдите "Git Bash" и запустите
   - Или кликните правой кнопкой мыши в любой папке и выберите "Git Bash Here"

2. **Введите команду:**
   ```bash
   git --version
   ```

3. **Ожидаемый результат:**
   ```
   git version 2.40.0.windows.1
   ```
   *Цифры версии могут отличаться, это нормально.*

4. **Проверьте работу в обычной командной строке (cmd):**
   - Нажмите `Win + R`, введите `cmd`, нажмите Enter
   - Введите `git --version`
   
   Если команда работает — значит, PATH настроен правильно. Если нет — перезапустите командную строку.

---

## ⚙️ Первоначальная настройка (обязательно!)

Git нужно "представиться" — указать ваше имя и email. Эти данные будут подписывать каждый ваш коммит.

**Откройте Git Bash** и выполните:

```bash
# Укажите ваше имя (можно на латинице или кириллице)
git config --global user.name "Иван Петров"

# Укажите ваш email (обязательно тот, который используется на GitHub/GitLab)
git config --global user.email "ivan.petrov@example.com"
```

**Почему это важно?**
- Без этого Git не даст сделать коммит
- Email привязывает ваши коммиты к аккаунту на GitHub

**Проверка настроек:**
```bash
git config --global --list
```
Вы должны увидеть ваши имя и email в списке.

---

## 🎨 Полезные настройки (рекомендуется)

### Цветной вывод в консоли
```bash
git config --global color.ui auto
```
*Git будет раскрашивать вывод команд — гораздо нагляднее.*

### Редактор по умолчанию
**Для Visual Studio Code:**
```bash
git config --global core.editor "code --wait"
```

**Для Notepad++ (популярный вариант на Windows):**
```bash
git config --global core.editor "'C:/Program Files/Notepad++/notepad++.exe' -multiInst -notabbar -nosession -noPlugin"
```

**Для Sublime Text:**
```bash
git config --global core.editor "'C:/Program Files/Sublime Text 3/sublime_text.exe' -w"
```

### Удобные алиасы (короткие команды)
```bash
# Сокращения для часто используемых команд
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.ci commit
git config --global alias.st status
git config --global alias.unstage 'reset HEAD --'
git config --global alias.last 'log -1 HEAD'
git config --global alias.lg "log --oneline --graph --all --decorate"
```

**Что дают алиасы:**
- Вместо `git checkout` можно писать `git co`
- Вместо `git status` — `git st`
- `git lg` покажет красивый граф коммитов

### Настройка окончаний строк (CRLF)
Для Windows это критично важно:

```bash
# Автоматически конвертировать CRLF в LF при коммите и обратно при checkout
git config --global core.autocrlf true
```

*Это гарантирует, что в репозитории будут Unix-окончания (LF), а на диске — Windows-окончания (CRLF). Никаких конфликтов из-за переносов строк!*

### Настройка имени ветки по умолчанию
С 2020 года GitHub использует `main` вместо `master`:

```bash
git config --global init.defaultBranch main
```

*Теперь при создании нового репозитория (`git init`) основная ветка будет называться `main`.*

---

## 🔧 Работа в разных терминалах Windows

### Git Bash
Самый удобный вариант для Git. Поддерживает Linux-команды (`ls`, `grep`, `touch`).

### Командная строка (cmd)
Стандартная консоль Windows. Git-команды работают, но Linux-команды — нет.

### PowerShell
Современная оболочка от Microsoft. Git работает отлично, но синтаксис команд отличается.

**Совет:** Для начала используйте **Git Bash** — там меньше сюрпризов.

---

## 🏁 Проверка итоговой конфигурации

Чтобы увидеть все глобальные настройки сразу:

```bash
git config --global --list
```

Пример вывода:
```
user.name=Иван Петров
user.email=ivan.petrov@example.com
color.ui=auto
core.autocrlf=true
init.defaultBranch=main
alias.co=checkout
alias.br=branch
alias.ci=commit
alias.st=status
alias.lg=log --oneline --graph --all --decorate
```

---

## ❓ Частые проблемы и их решение

### Проблема: "git не является внутренней или внешней командой"
**Причина:** Git не добавлен в системную переменную PATH.
**Решение:**
1. Найдите папку установки Git (обычно `C:\Program Files\Git\bin`)
2. Добавьте её в PATH:
   - Нажмите `Win + R`, введите `sysdm.cpl`
   - Вкладка "Дополнительно" → "Переменные среды"
   - В "Системные переменные" найдите `Path`, нажмите "Изменить"
   - Добавьте `C:\Program Files\Git\bin` и `C:\Program Files\Git\cmd`
   - Перезапустите терминал

### Проблема: "warning: LF will be replaced by CRLF"
**Причина:** Настройки core.autocrlf не соответствуют содержимому файлов.
**Решение:** Это предупреждение, а не ошибка. Git просто конвертирует окончания строк. Если хотите убрать — выполните:
```bash
git config --global core.autocrlf true
```

### Проблема: Не работает копирование/вставка в Git Bash
**Решение:** В Git Bash работают стандартные сочетания:
- `Ctrl + Insert` — копировать
- `Shift + Insert` — вставить
- Правый клик мышью тоже работает как вставка

### Проблема: Коммиты на GitHub привязаны не к моему аккаунту
**Причина:** Указан не тот email, который зарегистрирован на GitHub.
**Решение:** Проверьте email:
```bash
git config --global user.email
```
Если email не совпадает с GitHub — исправьте:
```bash
git config --global user.email "ваш-email@на-гитхабе.com"
```

---

## 📌 Итог

После выполнения всех шагов:
- ✅ Git установлен на Windows
- ✅ Проверена работа в Git Bash и командной строке
- ✅ Указаны имя и email
- ✅ Настроены окончания строк (CRLF)
- ✅ Добавлены удобные алиасы
- ✅ Настроено имя ветки по умолчанию (`main`)

**Git полностью готов к работе на вашем компьютере!**

---

<div align="center">
  
[📚 В словарь](GLOSSARY.md) | [🏠 На главную](README.md)

</div>
