# 📖 Словарь терминов Git

<div align="center">

**Быстрый справочник основных понятий**

</div>

---

## А

### **Add** (добавить)
Команда, которая помещает изменения в staging area. В JetBrains: `Ctrl + Alt + A` или галочка в Commit tool window.

### **Alias** (алиас)
Короткое имя для длинной команды. Например, `git co` вместо `git checkout`.

---

## Б

### **Branch** (ветка)
Указатель на коммит. Позволяет вести параллельную разработку. Ветка `main` — основная.

---

## В

### **Checkout** (переключиться)
Переключение между ветками или коммитами. В JetBrains: `Ctrl + Shift + A` → `checkout`.

### **Cherry-pick** (вишня)
Взятие одного коммита из другой ветки. Как "сорвать вишенку с торта".

### **Clone** (клонировать)
Скачать удаленный репозиторий на компьютер.

### **Commit** (коммит)
Снимок состояния проекта в определенный момент. Имеет хеш, автора, дату и сообщение.

---

## Г

### **Git**
Распределенная система контроля версий. Создана Линусом Торвальдсом в 2005.

### **GitHub**
Веб-сервис для хостинга Git-репозиториев. Крупнейшая платформа для open-source.

---

## Д

### **Diff** (отличия)
Показывает изменения между версиями. В JetBrains: вкладка Diff в Commit tool window.

---

## И

### **Index** (индекс)
См. Staging area.

### **Init** (инициализировать)
Создать новый репозиторий в текущей папке.

---

## К

### **Commit message** (сообщение коммита)
Текст, описывающий изменения. Правило: "Что и зачем, а не как".

### **Conflict** (конфликт)
Ситуация, когда Git не может автоматически объединить изменения. Требует ручного решения.

---

## Л

### **Log** (журнал)
История коммитов. В JetBrains: вкладка Log в Git tool window.

---

## М

### **Main** / **Master** (главная)
Основная ветка проекта. Обычно содержит стабильную версию.

### **Merge** (слияние)
Объединение изменений из разных веток.

---

## О

### **Origin**
Стандартное имя для удаленного репозитория (откуда клонировали).

---

## П

### **Push** (отправить)
Отправить локальные коммиты на удаленный репозиторий.

### **Pull** (забрать)
Забрать изменения из удаленного репозитория и смержить с локальными.

### **Pull Request** (PR)
Запрос на вливание изменений в чужой репозиторий (GitHub).

---

## Р

### **Rebase** (перебазировать)
Перенос коммитов на новую базу. Альтернатива merge с чистой историей.

### **Remote** (удаленный)
Репозиторий на сервере, не на вашем компьютере.

### **Repository** (репозиторий)
Хранилище кода с историей изменений. Бывает локальный и удаленный.

### **Reset** (сбросить)
Отмена изменений. Бывает soft, mixed, hard.

---

## С

### **Staging area** / **Stage**
Область подготовки. Туда попадают файлы перед коммитом.

### **Stash** (прятать)
Временное сохранение незакоммиченных изменений. Чтобы переключиться на другую ветку.

---

## Х

### **Hash** / **SHA** (хеш)
Уникальный идентификатор коммита. Например: `a1b2c3d4e5f6...`

### **HEAD**
Указатель на текущий коммит. Где мы сейчас находимся.

---

## Ч

### **Commit hash** (хеш коммита)
Уникальный идентификатор коммита. Можно использовать для переключения.

---

## Ш

### **Git tool window**
Окно Git в JetBrains IDE. Alt + 9 (Windows/Linux) или Cmd + 9 (macOS).

---

## Э

### **.git**
Скрытая папка в корне репозитория. Хранит всю историю и настройки.

### **.gitignore**
Файл со списком того, что Git должен игнорировать (логи, кэш, сборки).

---

<div align="center">

[🏠 На главную](README.md) | [📘 К главе 1](INTRODUCTION.md)

</div>
